/**
 * Thin compatibility shim. The Admin layout now uses the role-driven
 * UnifiedDashboardLayout so every dashboard renders inside a single shell.
 */
export { default } from "./UnifiedDashboardLayout";
